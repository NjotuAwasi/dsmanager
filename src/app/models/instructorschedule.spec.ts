import { Instructorschedule } from './instructorschedule';

describe('Instructorschedule', () => {
  it('should create an instance', () => {
    expect(new Instructorschedule()).toBeTruthy();
  });
});
